const Server = require('./index');

describe('1', () => {
  let app;
  beforeAll(() => {
    app = new Server().app;
  });
  it('dummy test', () =>
    expect(true).toBe(true));
})

describe('2', () => {
  let app;
  beforeAll(() => {
    app = new Server().app;
  });
  it('dummy test', () =>
    expect(true).toBe(true));
})