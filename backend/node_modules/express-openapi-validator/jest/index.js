const express = require('express');
const openapivalidator = require('../dist');

const apiSpec = './api.yml';

module.exports = class ExpressServer {

  constructor() {
    this.app = express();

    new openapivalidator.OpenApiValidator({
      apiSpec,
    }).install(this.app);
  }
}